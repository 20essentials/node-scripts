const SITES = {
  randomSite: {
    url: 'https://...',
    imageClassName: '.item_download.border'
  }
};

const SITE = SITES['randomSite'];
export const URL_DOWNLOAD_IMAGES_FROM_URL = SITE.url;
export const IMAGE_CLASSNAME = SITE.imageClassName;
